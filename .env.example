# Azure DevOps Personal Access Token
# Required scopes: Work Items (Read), Analytics (Read)
# Create at: https://dev.azure.com/<org>/_usersSettings/tokens
AZDO_PAT=

# Optional: override the VictoriaMetrics endpoint used by `metrics export`
# (defaults to the value in config.yaml)
# METRICS_VM_URL=http://victoriametrics:8428
