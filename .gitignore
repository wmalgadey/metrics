# Secrets & local config (repo-root only — perses/config.yaml is checked in)
.env
/config.yaml

# Local data (DuckDB, raw API snapshots, VictoriaMetrics storage)
data/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/

# Tooling
.pytest_cache/
.ruff_cache/
